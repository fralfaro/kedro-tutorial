# credentials.yml
# Archivo local — nunca se commitea al repositorio (.gitignore).
#
# Aquí van credenciales sensibles según entorno:
#
# Base de datos (dev):
# db_dev:
#   host: localhost
#   port: 5432
#   database: titanic_dev
#   user: postgres
#   password: secret
#
# Base de datos (prod):
# db_prod:
#   host: prod-server.internal
#   port: 5432
#   database: titanic_prod
#   user: prod_user
#   password: ${DB_PROD_PASSWORD}   # lee desde variable de entorno
